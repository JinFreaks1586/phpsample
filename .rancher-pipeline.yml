stages:
- name: runscript
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: willymartana/phpsample
      pushRemote: true
      registry: index.docker.io
timeout: 60
branch:
  include:
  - master
